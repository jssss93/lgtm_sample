# ════════════════════════════════════════════════════════════════
# 로컬 K8s (Dapr 없이)
# Usage: helm install ... -f values-local-base.yaml -f values-local.yaml
# ════════════════════════════════════════════════════════════════

dapr:
  enabled: false
